import React from 'react'
import RegistrationForm from '../Forms/RegistrationForm'

export default function Registration() {
        return (
            <div>
                <h1 className="font-bold text-xl p-3 flex items-center justify-center">Sign Up</h1>
                <RegistrationForm />
            </div>
        )
    }
